<!--
  Auto-synced template from ma-provider-tools/wrappers/feature-spec.md.j2.
  Do NOT edit this file in the provider repo — edit the wrapper upstream.

  How to use this template:
    1. Copy this file to specs/todo/<NNNN>-<kebab-slug>.md where NNNN is
       a zero-padded 4-digit number, monotonic per repo.
    2. Fill in the frontmatter and the mandatory sections below.
    3. Move the file to specs/inprogress/ when work begins (only one spec
       may live there at a time — WIP=1).
    4. Move to specs/done/ when the PR that implements it merges.

  Picking a size:
    - S  ≤ 10 min effort:  Problem Statement + Acceptance Criteria + Test Plan
    - M  10–20 min effort: S + Sequence Diagram
    - L  > 20 min effort:  M + Data Model
-->
---
id: "NNNN"
title: "<one-line title>"
size: S          # S | M | L
status: todo     # todo | inprogress | done
priority: P1     # P0 | P1 | P2
effort_minutes: 0
feature_id:      # optional; must match a ProviderFeature enum member if set
---

## Problem Statement

<What does a user (or upstream MA) observe today that is broken or missing?
 Stay in user-observable language; avoid internal class names.>

## Solution Summary

<One paragraph. Describe what changes from the outside in.>

## Acceptance Criteria

1. <Bullet 1 — must be falsifiable, ideally testable.>
2. <Bullet 2>
3. <Bullet 3>
4. <Bullet 4>
5. <Bullet 5 — minimum five per Feature Specification Discipline.>

## Test Plan

- <Unit test name and what it pins.>
- <Integration / snapshot test — what fixture? what assertion?>
- <Manual verification step, if any.>

<!--
## Sequence Diagram

Required for size: M and L. Use PlantUML or Mermaid:

```mermaid
sequenceDiagram
    User->>Provider: action
    Provider-->>User: result
```
-->

<!--
## Data Model

Required for size: L. Describe the dataclasses / TOML / JSON shapes the
feature introduces or modifies. Mark which fields are new, which change,
and which are deprecated.
-->
