# Auto-generated by ma-provider-tools — do not edit manually.
name: Version Guard

# No `paths:` filter on purpose: the job must report a status on every PR so it
# can be a required status check (a path-filtered check never reports on PRs
# that don't touch the path and would block them forever).
on:
  pull_request:
    branches:
      - dev

permissions:
  contents: read

jobs:
  version-guard:
    uses: trudenboy/ma-provider-tools/.github/workflows/reusable-version-guard.yml@main
    with:
      version_path: provider/VERSION
