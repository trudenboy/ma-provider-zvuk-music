---
title: Библиотека и синхронизация
description: Двунаправленная синхронизация библиотеки между Zvuk Music и Music Assistant
---

Провайдер синхронизирует библиотеку пользователя между Zvuk Music и Music Assistant в обоих направлениях.

## Что синхронизируется

| Тип | Из Zvuk → MA | Из MA → Zvuk |
|-----|:------------:|:------------:|
| Треки | ✅ | ✅ |
| Альбомы | ✅ | ✅ |
| Исполнители | ✅ | ✅ |
| Плейлисты | ✅ | ✅ |

## Как работает синхронизация

### Zvuk → MA (загрузка библиотеки)

При первом подключении и при каждом обновлении библиотеки MA:

1. Провайдер запрашивает коллекцию пользователя: списки ID треков, альбомов, исполнителей, плейлистов
2. ID разбиваются на батчи по 50 (лимит API)
3. Для каждого батча выполняется полный запрос данных
4. Объекты парсятся и добавляются в библиотеку MA

### MA → Zvuk (добавление/удаление)

| Действие в MA | Что происходит в Zvuk |
|---------------|-----------------------|
| Добавить трек в библиотеку | `like_track()` |
| Удалить трек | `unlike_track()` |
| Добавить альбом | `like_release()` |
| Удалить альбом | `unlike_release()` |
| Добавить исполнителя | `like_artist()` |
| Удалить исполнителя | `unlike_artist()` |
| Добавить плейлист | `like_playlist()` |
| Удалить плейлист | `unlike_playlist()` |

## Управление плейлистами

Помимо синхронизации, поддерживается полное управление плейлистами:

| Операция | Поддержка |
|----------|:---------:|
| Создать плейлист | ✅ |
| Добавить треки в плейлист | ✅ |
| Удалить треки из плейлиста | ✅ |

:::note[Удаление треков из плейлиста]
Zvuk API не поддерживает удаление трека по ID. Провайдер обходит это: получает все треки плейлиста, исключает нужные позиции, затем обновляет весь плейлист с оставшимися ID.
:::

## Примечания

- Ошибки при добавлении/удалении из библиотеки возвращают `False` вместо исключения (не прерывают работу MA)
- Синхронизация библиотеки не кэшируется — каждый запрос MA возвращает актуальные данные из Zvuk
